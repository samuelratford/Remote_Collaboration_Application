// This code is written by Samuel Ratford in its entirety

import React from 'react'; // React is not directly used but must be in the scope of the file to use JSX
import ReactDOM from 'react-dom';
import App from './App'; // Importing the application from ./App

ReactDOM.render(<App />, document.getElementById('root'));